{
  "name": "antavid",
  "version": "0.20.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/engine.js && node --check src/data.js && node --check sw.js"
  },
  "engines": {
    "node": ">=20"
  }
}
